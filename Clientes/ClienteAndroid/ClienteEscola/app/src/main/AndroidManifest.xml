<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unicamp.clienteescola" >

    <uses-permission android:name="android.permission.INTERNET" /> <!-- para acessar servidor -->
    <!--<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>-->

    <application android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".activities.consultar_alunos.ActivityConsultarAlunos" />
        <activity android:name=".activities.consultar_aluno.ActivityConsultarAluno" />
        <activity android:name=".activities.alterar.ActivityAlterar" />
        <activity android:name=".activities.inserir.ActivityInserir" />
        <activity android:name=".activities.excluir.ActivityExcluir"/>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>